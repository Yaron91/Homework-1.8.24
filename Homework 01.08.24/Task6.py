print("The meaning of the use of global in a function, it to call a defined parameters, outside of the function, that may have been redefined in the function itself. The changes will affect the defined parameter outside of the loop in case the global fnction was used.")
print("The reason for the error is you are trying to print the x inside the function 'foo' before definying it ")

